/**
 * Application Configuration
 */

/**
 * Data source URL
 *
 * IMPORTANT: The file in the GitHub repo uses Git LFS (Large File Storage).
 * The raw GitHub URL returns a pointer file, not the actual data.
 */
export const DATA_SOURCE_URL =
  "https://media.githubusercontent.com/media/chanduusc/Ui-Demo-Data/main/ui_demo.json";

export const config = {
  dataSourceUrl: DATA_SOURCE_URL,
};
